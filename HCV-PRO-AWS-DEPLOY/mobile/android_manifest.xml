<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hcvpro.openclaw.mobile"
    android:versionCode="1"
    android:versionName="1.0.0">

    <!-- HCV PRO - OpenClaw Mobile Augmented -->
    <!-- Transformez votre téléphone en mobile haut de gamme -->
    
    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />
    
    <!-- Permissions essentielles pour OpenClaw + HCV PRO -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Permissions OpenClaw avancées -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" />
    
    <!-- Hardware requirements pour mobile augmenté -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:glEsVersion="0x00030000"
        android:required="true" />

    <application
        android:name=".HCVProApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_hcv_pro_launcher"
        android:label="@string/app_name"
        android:theme="@style/HCVProTheme"
        android:requestLegacyExternalStorage="true"
        android:preserveLegacyExternalStorage="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:extractNativeLibs="true">

        <!-- Service principal HCV PRO + OpenClaw -->
        <service
            android:name=".services.HCVProOpenClawService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync|mediaProcessing"
            android:stopWithTask="false" />

        <!-- Service de compression automatique -->
        <service
            android:name=".services.CompressionService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProcessing" />

        <!-- Service OpenClaw integration -->
        <service
            android:name=".services.OpenClawIntegrationService"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync" />

        <!-- Assistant IA HCV PRO -->
        <service
            android:name=".services.HCVProAIAssistant"
            android:enabled="true"
            android:exported="true"
            android:foregroundServiceType="dataSync" />

        <!-- Accessibility Service pour OpenClaw -->
        <service
            android:name=".services.OpenClawAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Notification Listener pour OpenClaw -->
        <service
            android:name=".services.OpenClawNotificationService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Activité principale -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard|navigation|uiMode|density">
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Intent filters pour partage vers HCV PRO -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>
            
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>

        <!-- Activité des paramètres -->
        <activity
            android:name=".activities.SettingsActivity"
            android:exported="true"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Activité tableau de bord -->
        <activity
            android:name=".activities.DashboardActivity"
            android:exported="true"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Activité assistant IA -->
        <activity
            android:name=".activities.AIAssistantActivity"
            android:exported="true"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Fournisseur de contenu pour les fichiers compressés -->
        <provider
            android:name=".providers.HCVProFileProvider"
            android:authorities="com.hcvpro.openclaw.mobile.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Broadcast receivers -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.MY_PACKAGE_FIRST_LAUNCH" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".receivers.MediaScanReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_SCANNER_FINISHED" />
                <data android:scheme="file" />
            </intent-filter>
        </receiver>

        <!-- Meta-données OpenClaw -->
        <meta-data
            android:name="openclaw.api.version"
            android:value="2.1.0" />
        <meta-data
            android:name="openclaw.integration.enabled"
            android:value="true" />
        <meta-data
            android:name="hcvpro.compression.engine"
            android:value="augmented" />
        <meta-data
            android:name="hcvpro.device.class"
            android:value="premium" />

    </application>

</manifest>
