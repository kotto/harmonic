# KA Phone — Dépendances réelles
# ================================

# Core
numpy>=1.24.0
scipy>=1.10.0

# Web Server
flask>=2.3.0
flask-cors>=4.0.0

# IA Locale (LLM GGUF)
llama-cpp-python>=0.2.0

# Mathématiques symboliques (Calculateur Harmonique)
sympy>=1.12

# Réseau (DeepSeek API fallback)
requests>=2.28.0

# Speech — STT (Speech-to-Text)
faster-whisper>=0.10.0

# Speech — TTS (Text-to-Speech)
edge-tts>=6.1.0
piper-tts>=1.2.0

# Speech — VAD (Voice Activity Detection) + Audio
torch>=2.0.0
sounddevice>=0.4.6

# Image / Vision (HCV)
Pillow>=10.0.0

# Optionnel — Génération vidéo
opencv-python>=4.8.0