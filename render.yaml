services:
  - type: web
    name: ka-mobile
    env: python
    rootDir: engine
    buildCommand: |
      pip install -r requirements.txt
      cd ../ka-mobile-android && npm ci && npm run build
    startCommand: python -m ka_server.app --port $PORT
    envVars:
      - key: VITE_API_URL
        value: https://ka-mobile.onrender.com
      - key: PYTHONUNBUFFERED
        value: "1"
  - type: web
    name: hcv2-pro-www
    env: static
    buildCommand: cd engine && cp -r www/* .
    staticPublishPath: engine/www
    domain: hcv2.pro
  - type: web
    name: hcv2-pro-api
    env: python
    buildCommand: cd engine && pip install -r requirements.txt
    startCommand: ./engine/start_api.sh
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: 8765
    healthCheckPath: /api/health
