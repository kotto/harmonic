# Render Requirements — KA Web Complete v3
# Moteur ondulatoire + voix + API

# Core
numpy>=1.24
psutil>=5.9

# Voice (Edge-TTS pour qualité, Piper pour offline)
edge-tts>=6.1
flask>=2.3
flask-cors>=4.0

# Spectral embedding (PPMI + Laplacian)
scipy>=1.11

# Optional: Piper TTS (local, 50 MB)
# pip install piper-tts  # décommenter si espace dispo suffisant
