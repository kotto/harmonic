{
  "name": "ka-care-android",
  "version": "1.0.0",
  "description": "KA CARE — Application Android (wrapper Capacitor de l'app web de dépistage communautaire)",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-assets.mjs && npx cap sync android",
    "assets": "node scripts/sync-assets.mjs",
    "build": "npm run sync && cd android && gradlew assembleDebug",
    "apk": "npm run build"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}