# ──────────────────────────────────────────────
# SSL Config Template (généré par certbot)
# ──────────────────────────────────────────────
# Ce fichier sera créé automatiquement par certbot
# Placez vos domaines dans docker-compose.yml → certbot
# Puis lancez: docker compose run --rm certbot certonly --webroot -w /var/www/certbot -d admin.vitalka.org -d api.vitalka.org