{
  "name": "vital-ka-android",
  "version": "2.1.0",
  "description": "Vital Ka — Application Android (wrapper Capacitor de l'app web)",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-assets.mjs && npx cap sync android",
    "assets": "node scripts/sync-assets.mjs",
    "build": "npm run sync && cd android && gradlew assembleDebug",
    "apk": "npm run build"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor-community/speech-recognition": "^7.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0"
  }
}
