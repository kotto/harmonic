"""
Frontend Engine — Moteur de génération frontend harmonique (autonome)
======================================================================
Dossier dédié regroupant tous les modules de génération de code frontend
basés sur la théorie harmonique (φ, ondes, Fibonacci, HRR).

MODULES :
  1. css_harmony.py       → CSS harmonique (typo φ, Fibonacci, couleurs par ondes)
  2. code_generator.py    → 50+ templates HTML/CSS/JS (standalone, sans dépendance)
  3. phi_flex.py          → Flexibilité φ (variantes, enrichissements, composition)
  4. real_composers.py    → Fusion HTML réelle, pages multi-sections, analyse référence
  5. enhance_frontend.py  → JS interactif, thème contextuel, design polish
  6. image_to_code.py     → Analyse image (Pillow) → palette + layout
  7. js_state_machine.py  → États UI (idle/loading/success/error/empty)
  8. benchmarks/          → Benchmark frontend + comparaison LLMs + projection Arena

CARACTÉRISTIQUES :
  - 0 dépendance externe (sauf numpy pour les vecteurs, PIL optionnel)
  - 0 paramètre, 0 GPU, 0 coût
  - Déterministe à 100% (φ-seeded hashing)
  - 0% hallucination (templates garantis valides)
"""
